# v0.2.3（build 9）私有发行记录

- Git tag：`v0.2.3`
- 发行 commit：待交付后回填
- DMG SHA-256：待交付后回填
- 分发范围：指定用户私有下载；Apple Development 签名，不公开发布。

## 用户可见变化

- 修复 iCloud 同步在子任务改名、撤回延期或编辑重复计划子任务后陷入「同步未完成」确定性失败循环的问题：同步合并层曾把这些合法可变字段当作不可变身份，导致上传被反复拒绝、本机变更无法上传（FAIL-2026-08-06-01，同为 FAIL-2026-08-04-22 的根本修复）。安装本版后第一轮同步自动恢复，无需删除同步目录或重装。
- 根目录 README 改为默认英文版并提供中文版 `README.zh-CN.md` 互相切换；英文截图由演示 fixture 的英文故事变体真实生成。

## 资料兼容

- SQLite：当前 schema v17；受控 v15 原地升级至 v17，保留既有资料与同步 journal。本版无数据格式变更。
- JSON：严格 canonical v6 数据包可导入；当前导出格式为 v7。
- 未知 schema、未知 format 与非 canonical 输入维持 fail-closed。

## 验证摘要

待交付后回填。

## 灰度与回滚

只向指定用户提供该 checksum 的 DMG。若出现资料、同步或关键交互回归，立即停止继续提供此 DMG，保留用户资料，由用户决定恢复路径；不得自动降级、删除或迁移 production 资料。本版无数据格式变更，回滚至 v0.2.2 (8) 安全（回滚会恢复子任务合并身份 guard 的毒记录风险）。
