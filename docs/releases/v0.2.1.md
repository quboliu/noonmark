# v0.2.1（build 6）私有发行记录

- Git tag：`v0.2.1`
- 发行 commit：`5f4d6cdcf0d39e38a47ee128a9e6102e13b58d80`
- DMG SHA-256：`05f5fd62c1539ec2c3d083d42f442237a78974c3731584fb5b0dfe01c4a69fe0`
- 分发范围：指定用户私有下载；Apple Development 签名，不公开发布。

## 用户可见变化

- 飞光（Flylight）成为一等记录能力；札记英文展示名为 Memo。
- iCloud 同步合并拒绝补充隐私安全的规则级诊断码。
- 全局录入、Day Todo 与设置页的真实 WindowServer 交互稳定性修复。

## 资料兼容

- SQLite：受控 schema v15 原地升级至当前 v17，保留既有资料与同步 journal。
- JSON：严格 canonical v6 数据包可导入；当前导出格式为 v7。
- 未知 schema、未知 format 与非 canonical 输入维持 fail-closed。

## 验证摘要

- `make check`：通过，审计清单 `suite_exit_status=0`。
- 完整真实 App E2E 与年度 Demo fixture：通过。
- DMG：签名、版本、Mach-O UUID、dSYM、SHA、来源绑定静态验证通过。
- 安装：仅运行从 DMG 受控派生并重签名的 `dmg-validation` App；`production_app_executed=false`、`validation_app_executed=true`、`suite_exit_status=0`。

## 灰度与回滚

只向指定用户提供该 checksum 的 DMG。若出现资料、同步或关键交互回归，立即停止继续提供此 DMG，保留用户资料，由用户决定恢复路径；不得自动降级、删除或迁移 production 资料。

