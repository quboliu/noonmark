# v0.2.2（build 7）私有发行记录

- Git tag：`v0.2.2`
- 发行 commit：待回填
- DMG SHA-256：待回填
- 分发范围：指定用户私有下载；Apple Development 签名，不公开发布。

## 用户可见变化

- 修复年度数据规模下「已完成」页切换卡顿 5-6 秒：已完成池投影由每链全表扫描改为一次预分组共享（FAIL-2026-08-05-01）。
- 修复 Day Todo、任务池等页面切换可感知延迟：页面投影、任务行查询、侧边栏计数与右栏统计统一按数据版本备忘，同一版本只计算一次（FAIL-2026-08-05-02）。修复后十个一级页面切换实测 79-205ms。
- 飞光速记占位文案修正：不再把每条笔记称为「飞光」。

## 资料兼容

- SQLite：受控 schema v15 原地升级至当前 v17，保留既有资料与同步 journal。
- JSON：严格 canonical v6 数据包可导入；当前导出格式为 v7。
- 未知 schema、未知 format 与非 canonical 输入维持 fail-closed。

## 验证摘要

- `make check`：通过，审计清单 `suite_exit_status=0`。
- 页面切换耗时门禁：十个一级页面每页 < 1500ms、TOTAL 1414ms（`page-switch-latency.tsv`）。
- 完整真实 App E2E 与年度 Demo fixture：待本版发行链回填。
- DMG 静态验证与受控安装：待本版发行链回填。

## 灰度与回滚

只向指定用户提供该 checksum 的 DMG。若出现资料、同步或关键交互回归，立即停止继续提供此 DMG，保留用户资料，由用户决定恢复路径；不得自动降级、删除或迁移 production 资料。本版无数据格式变更，回滚至 v0.2.1 (6) 安全。
