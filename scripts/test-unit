#!/usr/bin/env bash
set -euo pipefail

scripts/reset-dev-data audit
swift test --filter NoonmarkCoreTests
swift test --filter NoonmarkSyncTests
swift test --filter IdeaCaptureSessionTests
swift test --filter NewTaskDraftParserTests
swift test --filter RevisionMemoTests
swift test --filter NoonmarkAITests
swift test --filter NoonmarkMacUIContractTests
