# 晷迹 Mac UI 设计契约

本文把 `docs/design/prototypes/claude-mac-ui-2026-07-05/` 中的 Claude Design 原型拆解成实现契约。Mac 端实现必须逐项落实，不能遗漏、糊弄或暗改。

## 全局窗口与视觉

- macOS 窗口外壳：顶部 traffic lights、居中窗口标题、应用时钟 Logo、浅色桌面背景、白色/浅灰内容基底。
- 窗口标题必须从当前页面和选中日期派生；Day Todo 与日历页显示当前选中日期，不得硬编码为固定应用名。
- 主窗口尺寸基准：原型预览 `1440 x 900`，窗口最大内容约 `1400 x 880`。
- 主题：`冷灰` 与 `微暖纸感` 两套外观。
- 语言：中文与 English 两套文案入口。
- 全局动效：日期 strip 选中态平滑移动、toast rise 动效、hover 高亮。
- 全局颜色语义：accent、ok、warn、chip、panel、line、t1/t2/t3。
- 状态样式必须使用共享映射：待完成使用 accent/accent-soft，已完成使用 ok/ok-soft，未完成使用 warn/warn-soft，已延续、已变更、已回池使用 chip/t2，已废弃使用 chip/t3；任务行、日历、详情栏和轨迹时间线不得各自定义状态颜色。未完成状态色必须对应原型 `--warn` / `--warn-soft`，不得混用侧栏未完成导航橙色。
- 列表选中态必须使用 accent/accent-soft，不得被任务状态色覆盖；状态色只用于 glyph、chip、进度和轨迹节点。
- 非编辑态完成进度必须使用确定性自绘进度条，100% 使用 ok 绿色，避免 macOS 系统 `ProgressView` 把完成态渲染成灰色。
- 右键菜单、日期选择弹窗、变更任务弹窗、toast 都是必需交互。
- 以上窗口尺寸、颜色语义和全局动效都必须进入 `SuntraceMacUIContract`，不能只停留在视觉说明里。

## 左侧导航

导航分组必须保留：

- `计划`
  - `Day Todo`，显示今天待完成数量。
  - `任务池`，显示任务池数量。
  - `未来计划`，显示未来计划数量。
- `轨迹`
  - `未完成`，显示未完成任务链数量。
  - `已完成`，显示完成记录数量，包含子任务完成记录。
  - `日历`。
  - `烛龙 AI`，进入可选 AI sidecar，不影响普通清单能力。
- 底部 `设置`。

导航项必须有图标、选中背景、左侧选中条、计数胶囊和 hover 反馈。
导航图标必须保留原型的页面语义色，不得被选中态或统一文字色覆盖：Day Todo `#2A6FDB`、任务池 `#0E9488`、未来计划 `#7C5CFF`、未完成 `#E0851B`、已完成 `#1F8A5B`、日历 `#D1477A`、烛龙 AI `#7C5CFF`、设置 `#64748B`。
关键操作按钮必须保护完整文案宽度，例如“从任务池排期…”不得被截断成省略号。

## Day Todo 页面

必须包含：

- 日期标题、星期、今日/历史已锁定/未来 badge。
- 上一天、今天、下一天、选日期按钮。
- 14 天日期 strip，含星期、日期、今天描边、选中圆点、任务存在点。
- 今天描边、选中圆点、任务存在点必须作为日期 strip 的独立契约点保留，不能被泛化成一个日期条组件后遗漏。
- 日期 strip 的视觉选中 pill 和全局平滑移动动效是两个契约点：前者属于 Day Todo 元素，后者属于全局动效。
- 日期 strip 选中 pill 必须是容器级独立滑动层，按 14 天 strip 的选中 index 平移；日期格子只负责文字、今天描边和任务存在点，不能退化为每个格子各自绘制本地选中圆。
- 历史日锁定提示：任务事实不可改写，可补写复盘；未完成任务可延续或废弃。
- 未来日提示：可排期与调整顺序，不能提前完成或生成复盘。
- 当前日和未来日的快速新增输入。
- 从任务池排期按钮。
- 任务行：选择态、状态圆点、标题、进度条、延续/持续/变更/回池元信息、子任务展开、状态 chip、上下移动按钮。
- 任务行的选择态、状态圆点、标题、轨迹元信息和上下移动按钮必须作为独立契约点保留。
- 当日优先级控件表达领域里的当日优先级；任务行上下移动按钮是该控件在列表行中的视觉入口，两者都必须有实现映射。
- 空状态插图与文案。
- 所有空状态都必须同时保留插图和文案，不能只显示一个空容器。
- Day Todo、任务池、未来计划、未完成池、已完成池和日历的空状态都必须分别保留插图与文案契约点。

任务行交互必须包含：

- 当前日待完成且无子任务时可点击圆点完成。
- 当前日/未来日待完成可通过上下按钮调整当日优先级。
- 右键菜单与详情页三点菜单共享动作。
- 右键菜单必须按页面、日期、状态和子任务情况过滤动作：当前待完成可延续、变更、回池、废弃，且仅无子任务时可直接完成；当前已完成仅无子任务时可撤销完成；历史未完成且没有活跃轨迹时可延续或废弃；历史已完成只可复制为新任务；未来待完成只可改期或回到任务池。
- 有子任务的任务行可展开子任务，并显示子任务完成态和难度权重。

## 任务池页面

必须包含：

- 标题、说明、新建任务输入。
- 任务池任务列表。
- 任务池行首必须是 18px 灰色虚线空圆，占位语义为“尚未排期”，不得替换成彩色任务池图标。
- 每行直接提供排期到今天、排期到明天、选日期。
- 空状态。

## 未来计划页面

必须包含：

- 按日期分组展示未来计划。
- 每个日期组可跳转到对应 Day Todo。
- 日期组标题中的“查看当天”链接必须紧跟日期标题，使用原型的 10px 左右间距；不得使用 spacer 推到分组行最右侧。
- 未来计划任务可选中、右键、查看详情、改期、回到任务池、调整顺序。
- 未来计划任务的选中、右键和查看详情必须作为独立交互契约点保留。
- 未来计划任务的调整顺序必须同时映射到 `MacUIFuturePlanElement.priorityStepper` 和行内入口 `MacUIFuturePlanElement.taskRowMoveButtons`；两者描述同一个排序能力的语义控件与视觉入口。
- 空状态。

## 未完成池页面

必须包含：

- 按任务链去重展示未完成轨迹。
- 每条展示未完成次数、延续次数、最近未完成日期。
- 未完成行元信息必须按原型使用纯文本与 `·` 分隔：未完成次数为 warn 色文本，延续次数和“最近未完成 · 日期”为 t3 文本；不得把未完成次数退化为胶囊 chip 或省略“最近未完成”标签。
- 未完成行的“明细”展开入口必须位于元信息行最右侧，和失败次数、延续次数、最近未完成日期同一行；不得放到标题行或操作按钮列。
- 若已有活跃日轨迹，显示“已延续到某日，当前待完成”并可跳转。
- 若无活跃日轨迹，提供延续到某日和废弃任务链。
- 可展开明细，逐条显示未完成日期、序号、已延续到哪天。
- 打开详情时使用通用任务详情栏：标题为“任务详情”，包含未完成状态、日期、延续上下文、历史只读提示、完成进度和任务轨迹；延续、废弃和跳转动作保留在列表行，不在详情栏重复堆叠专用处理区。
- 空状态。

## 已完成池页面

必须包含：

- 按完成日期分组。
- 父任务完成记录逐条展示。
- 子任务完成记录也要单独展示，并显示属于哪个父任务。
- 每条完成记录展示轨迹节点。
- 每条支持跳转当天、复制为新任务。
- 父任务完成记录可在右上角显示完成时间；子任务完成记录右上角只显示“子任务”标签，不显示额外完成时间，保持原型 `time:''` 的信息密度。
- 父任务完成记录打开详情时使用通用任务详情栏：标题为“任务详情”，包含完成进度、描述、附言和任务轨迹；跳转当天和复制为新任务保留在右上角三点菜单，不额外显示统计卡、锁定提示、子任务快照或底部重复操作区。
- 空状态。

## 日历页面

必须包含：

- 月标题、上一月、今天、下一月。
- 周一开始的月份网格。
- 每日格子展示日期、今天标记、完成热度块、最多 6 条任务摘要和更多数量。
- 任务摘要使用状态色点，完成/废弃使用删除线。
- 右侧选中日详情：日期、星期、今日/历史/未来 badge、任务总数、打开 Day Todo。
- 右侧选中日任务列表和空状态。
- 右侧选中日任务行必须复用 Day Todo 的轨迹元信息语义：延续任务显示“第 N 次延续 · 持续 X 天”，变更任务显示变更提示，不得退化为只显示延续次数。

## 设置页面

必须包含：

- 外观分段控件：冷灰、微暖纸感。
- 语言分段控件：中文、English。
- 数据区：导出数据 `JSON`、导入数据。
- 同步区：自定义同步端点、iCloud 云同步，均显示规划中。

## 右侧详情栏

未选中任务时：

- Day Todo 页面显示每日复盘。
- 任务池、未来计划、未完成池、已完成池显示页面提示文案。
- 通用右侧详情栏的未选中提示必须是轻量 rail hint，不得复用页面级大图标空状态。
- 日历和设置不显示通用详情栏。
- 日历页面不显示通用任务详情栏，但必须显示自己的选中日详情面板；设置页面不显示右侧详情栏。

选中任务时必须包含：

- 详情标题、关闭按钮、任务标题、三点菜单。
- 详情标题和任务标题必须分别实现，不能合并为一个视觉元素。
- 三点菜单必须是 22px 纯图标触发器，不得显示文字省略号或原生下拉箭头。
- 状态 chip、所属日期或任务池位置。
- 轨迹上下文卡片：以单行紧凑展示延续次数、持续天数、变更、回池等，不得退回多行统计卡。
- 历史只读提示。
- 完成进度区。
- 描述区。
- 附言区。
- 子任务区。
- 任务轨迹时间线；详情栏章节标题显示为“任务轨迹”。
- 通用任务详情必须在任务轨迹标题行或紧邻的上下文卡中显示链条摘要；已有上下文卡时不得在同一详情栏重复显示同一组摘要。已完成详情的任务轨迹标题行必须显示持续天数和终态 chip。
- 任务轨迹的当前节点必须有 accent-soft 选中背景，并使用细边框“当前所在”badge；不能只依赖填充式“当前”文字 chip。

## 进度规则

- 无子任务的任务可使用手动完成进度。
- 手动完成进度滑杆必须使用确定性自绘轨道、蓝色已完成填充、可见 thumb 和 5% 步进刻度；不得依赖 macOS 系统 Slider 的默认灰色渲染。
- 手动完成进度滑杆必须暴露可访问调节入口，递增/递减按 5% 步进并遵守进度下限。
- 详情栏完成进度区域不显示“手动完成进度”或“按子任务自动计算”模式胶囊；模式信息只保留在控件语义和可访问说明中。
- 只读完成进度标题和百分比必须在同一行，轨道位于下一行；可编辑手动滑杆保留已验证的标题、百分比和滑杆垂直节奏。
- 有子任务的任务进度必须按子任务自动计算。
- 子任务有难度权重：简/中/难，对应 English `S/M/L`。
- 延续后的任务进度不能低于历史已达进度，UI 显示下限。
- 进度达到 100% 后 UI 可以驱动完成态，但落库仍必须经过领域接口。
- 历史日进度只读。

## 描述与附言

- 任务详情必须分为 `描述` 和 `附言`。
- 描述用于背景、目标或范围。
- 附言用于临时想法、提醒或补充说明，视觉上是浅暖色、斜体。
- 当前日、未来计划和任务池可编辑。
- 历史日只读。
- 这些字段不能覆盖式改写历史日轨迹。

## 子任务规则

- 子任务可完成、显示完成日期、显示锁定图标。
- 历史已完成子任务不可在后续日期改写。
- 子任务权重可在当前日编辑。
- 延续时只延续未完成子任务，已完成子任务留在历史轨迹中。
- 任务轨迹时间线必须显示当日完成的子任务和被延续的子任务。

## 每日复盘

- 每日复盘包含统计卡、状态分段条、状态图例、今日总结、未完成原因、明日注意事项。
- 当前日每日复盘区必须包含“让烛龙分析今日”入口，生成建议草稿后进入烛龙页由用户确认。
- 历史日复盘可补写。
- 未来日不显示复盘编辑，只显示未来日无复盘提示。
- 输入后显示自动保存反馈。

## 操作与弹窗

任务动作必须覆盖：

- 标记完成、撤销完成。
- 延续到某日。
- 变更为新任务。
- 回到任务池并留下轨迹。
- 废弃任务链。
- 改期。
- 复制为新任务。
- 排期到今天、明天或指定日期。

弹窗必须覆盖：

- 延续/排期/改期/跳转日期/从任务池排期的日期选择器。
- 变更任务弹窗：旧任务删除线展示，新任务标题输入，确认变更。
- 变更任务弹窗内的旧任务删除线预览、新任务标题输入和确认按钮必须作为独立契约点保留。
- 右键菜单。
- toast。

## 变更任务语义

- 旧任务必须保留在当天并标注已变更。
- 新任务必须以待完成出现在同一天。
- 新任务开启新的任务链，而不是继承旧任务链。
- 旧日轨迹通过变更指针关联到新日轨迹，用于 UI 展示“被变更为”。

## 撤销

- `Cmd+Z` 只允许撤销当前日与未来计划的误操作。
- 不能撤销历史事实。
- 任务池新增、复制为新任务、当前日延续、当前日废弃、未来改期、当前日任务事实编辑都必须进入有限撤销栈；历史日废弃、历史日延续等改写历史状态的动作不得进入撤销栈。
- 无可撤销操作时显示提示。
- 无可撤销操作时的提示必须作为独立 toast 契约点保留。

## 后端硬需求

原型要求后端必须支持：

- 任务描述和附言，且历史日不可被覆盖式改写。
- 手动完成进度。
- 子任务难度权重。
- 按子任务权重自动计算进度。
- 进度下限，防止延续任务进度回退。
- 已完成池中展示子任务完成记录。
- 日历月视图需要按日统计任务和完成热度。
- 设置中预留主题、语言、导入导出、同步端点和 iCloud 云同步。
- 任务池持久化视图必须包含已经 **回池** 的任务链。
- 设置偏好必须支持冷灰/微暖纸感、中文/English。
- 有限撤销能力必须支持 `Cmd+Z` 当前日与未来计划误操作，历史事实不可撤销。
- 变更任务必须保存旧日轨迹指向新日轨迹的变更指针。

## 当前实现状态

- 设计原型已归档。
- Core 已有 Day Todo、任务池、未来计划、未完成池、已完成池、每日复盘、子任务轨迹。
- Core 已补齐描述/附言、手动进度、子任务权重、加权进度、进度下限、日历汇总、已完成子任务记录和设置偏好模型。
- Storage 已补齐描述/附言、手动进度、子任务权重、已完成子任务记录 view、同步端点占位 view，并修正已回池任务链的任务池 view。
- Mac UI 契约已显式覆盖窗口尺寸、颜色 token、日期 strip 选中动效、日期 strip 视觉细节、任务行微观元素、逐页面空状态插图/文案、未来计划交互、详情栏标题/空态、子任务完成日期、子任务锁定图标、变更弹窗内部元素、撤销不可用 toast、变更指针和有限撤销。
- Mac SwiftUI 主窗口已覆盖 Day Todo、任务池、未来计划、未完成池、已完成池、日历、烛龙 AI 和设置页；`scripts/test-e2e` 已生成真实 macOS 页面截图并验证正常模式持久化。
- 设置页导出 / 导入入口已接入 `SuntraceDataPackage` JSON 数据包，Storage 测试覆盖 round-trip、重复键和断裂引用拒绝。
- 烛龙 Provider 配置表单已接入 App：非密配置保存到 UserDefaults，API Key 只进入 Keychain；OpenAI-compatible Provider 已支持 `/chat/completions` 和 `/models` 健康检查。
- 烛龙本地证据建议草稿和逐条确认应用流已接入 App；确认操作通过普通领域接口落库，`assignLabel` 在核心 label 模型存在前 fail-closed。
- 待补齐：更深的真实 UI 交互自动化、远程模型结构化操作解析、Apple Developer ID 签名和 notarization。
