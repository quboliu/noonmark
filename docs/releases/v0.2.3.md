# v0.2.3（build 9）私有发行记录

- Git tag：`v0.2.3`
- 发行 commit：`5763652ffe96dbc3d0fe22e0135dd02038bfa98c`
- DMG SHA-256：`44eff6dbe09654017e9cb87e57ac5b156ef13f9524e235daf45db31b38389903`
- 分发范围：指定用户私有下载；Apple Development 签名，不公开发布。

## 用户可见变化

- 修复 iCloud 同步在子任务改名、撤回延期或编辑重复计划子任务后陷入「同步未完成」确定性失败循环的问题：同步合并层曾把这些合法可变字段当作不可变身份，导致上传被反复拒绝、本机变更无法上传（FAIL-2026-08-06-01，同为 FAIL-2026-08-04-22 的根本修复）。安装本版后第一轮同步自动恢复，无需删除同步目录或重装。
- 根目录 README 改为默认英文版并提供中文版 `README.zh-CN.md` 互相切换；英文截图由演示 fixture 的英文故事变体真实生成。

## 资料兼容

- SQLite：当前 schema v17；受控 v15 原地升级至 v17，保留既有资料与同步 journal。本版无数据格式变更。
- JSON：严格 canonical v6 数据包可导入；当前导出格式为 v7。
- 未知 schema、未知 format 与非 canonical 输入维持 fail-closed。

## 验证摘要

- `make check`：通过（同一 evidence run `release-v0.2.3-b9-20260806T065612Z`），审计清单 `suite_exit_status=0`。
- 完整真实 App E2E 与 writer-lease E2E：同一 evidence run 通过，runtime 清单 `suite_exit_status=0`。
- 发行全链 `scripts/release-private-dmg`：版本 tag 精确绑定、静态 preflight、clean worktree 诊断闭环、腾讯拼音 release smoke、DMG 打包与只读静态验证（`production_app_executed=false`）、受控 `dmg-validation` 安装／退出／重启／诊断导出验证与全量证据对账全部通过。
- 故障案例门禁：51 案例通过；FAIL-2026-08-06-01（本次同步修复）与 FAIL-2026-08-04-22（根本修复回填）均已闭环并绑定 fast＋symptom 永久门禁。

## 灰度与回滚

只向指定用户提供该 checksum 的 DMG。若出现资料、同步或关键交互回归，立即停止继续提供此 DMG，保留用户资料，由用户决定恢复路径；不得自动降级、删除或迁移 production 资料。本版无数据格式变更，回滚至 v0.2.2 (8) 安全（回滚会恢复子任务合并身份 guard 的毒记录风险）。
