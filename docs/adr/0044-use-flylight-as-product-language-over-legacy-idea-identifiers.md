# 以飞光作为产品语言并保留旧 Idea 标识符

晷迹以 **飞光**（英文界面为 Flylight）作为独立速记能力唯一的用户语言与领域语言；导航、页面、操作、提示和规格不再使用「想法」、Memo 或 `Idea` 指代该能力。现有源码、SQLite、同步 payload 与稳定身份中的 `Idea*`、`idea_*` 属于已经发布的兼容标识符，本轮不做没有用户价值且会扩大迁移风险的连锁改名；只有单独迁移方案证明同步收敛、旧新能力对等和安全回滚后才能迁移这些标识符。
