# Sticky Note 引用飞光而不复制内容

Sticky Note 是活动飞光条目的精选投影，不建立第二份正文或独立编辑实体；加入／移出只改变精选关系，飞光仍留在完整时间线，编辑会同步出现在所有展示视图，删除则自动退出 Sticky Note。第一版沿用已经进入 SQLite 与同步协议的 `pinnedAt` 兼容字段承载精选时间，但产品不再呈现「置顶」语义；清单流、便签墙和后续便签纸视觉只属于可替换的本机展示层，不进入内容或同步事实。
