# v0.2.4（build 13）发行记录

- Git tag：待候选 tag 确定后回填
- 发行 commit：待交付后回填
- DMG SHA-256：待交付后回填
- 分发范围：公开 GitHub Release 下载；Apple Development 签名，没有 Developer ID、notarization 或 staple。

## 用户可见变化

- 收敛飞光右栏：移除重复正文和无独立价值的大型编辑入口；没有独立信息时保持留白。飞光页输入框改为默认完整展开，不再提供收起状态。
- 子任务难度入口改为复选框右侧的紧凑深浅圆点，保留原生菜单、键盘与 VoiceOver 语义；标题到删除按钮的间距缩至 `4 pt`，把横向空间还给正文。底层难度模型与持久化格式不变。
- Sticky Note slogan 改为「把值得反复看见的内容，留在手边。」；「飞光」只表示模块入口或内容来源，单条内容统一使用「记录／条目」与 `entry`。

## 资料兼容

- SQLite：当前 schema v17；继续支持受控 v15 原地升级至 v17。本版没有 schema、同步记录或领域资料格式变更。
- JSON：严格 canonical v6 数据包继续可导入；当前导出格式仍为 v7。
- 未知 schema、未知 format 与非 canonical 输入维持 fail-closed；本版不新增兼容层、迁移路径或回退机制。

## 验证摘要

待候选交付后回填。

## 灰度与回滚

先由本地完整验证链验证精确 candidate commit，再推送受保护 tag，由 GitHub-hosted runner 复跑非 GUI 门禁、签名、打包并公开 GitHub Release。若出现资料、同步或关键交互回归，立即撤下 Release 资产并保留用户资料；不得自动降级、删除或迁移 production 资料。本版没有资料格式变更，可回到上一已验证的 v0.2.3 (9)，但会恢复本版修复的飞光右栏、输入框、子任务布局与领域文案问题。
