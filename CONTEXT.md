# 晷迹领域上下文

晷迹（Noonmark）是一个围绕每日任务轨迹和每日复盘建立的 Todo 产品。本文记录领域专家与研发之间已经达成一致的项目语言，避免后续把核心概念误实现成普通待办事项。

## Language

**Day Todo**:
某一个自然日对应的任务工作台，承载该日被计划、执行、完成、延续或遗留的任务。
_Avoid_: 今日清单, 当日列表

**快速捕获**:
用户不经过 **烛龙会话** 或 **AI Provider** 即可立即把普通任务加入 **Day Todo** 或 **任务池** 的创建路径。
_Avoid_: AI 创建关卡, 强制规划, 保存前评估

**当日优先级**:
任务在某个 **Day Todo** 中由用户手动确定的执行顺序。
_Avoid_: 排序, 展示顺序

**查看排序**:
用户浏览 **Day Todo** 或聚合池时临时选择的展示顺序。
_Avoid_: 优先级, 任务顺序

**有限撤销**:
只允许撤销尚未固化为历史事实的当前日或计划草稿误操作。
_Avoid_: 全局撤销, 无限撤销

**Mac UI 设计契约**:
从 Claude Design Mac 原型逐项拆出的实现硬标准，覆盖页面、控件、状态、动效、视觉 token 和后端语义支撑。
_Avoid_: 视觉参考, 可选灵感, UI 草图

**白盒原则**:
晷迹必须向用户披露所有会影响其决定的数据使用、系统推断、假设、不确定性、自动化、状态变更和失败，并通过情境提示与 **烛龙事件历史** 提供可追溯说明和控制入口。
_Avoid_: 隐藏自动化, 黑盒评分, 弹窗轰炸

**任务池**:
尚未被安排到任何具体 **Day Todo** 的待排期任务集合。
_Avoid_: 待办箱, inbox, backlog

**已完成池**:
跨日期汇总所有 **已完成** 日轨迹的只读集合。
_Avoid_: 完成列表, 完成任务

**已完成轨迹摘要**:
**已完成池** 中每条完成记录附带的只读轨迹信息，展示该 **任务链** 从哪一天开始、延续到了哪几天、在哪一天完成。
_Avoid_: 可编辑履历, 完成详情

**已完成子任务记录**:
**已完成池** 中单独展示的子任务完成记录，标注它属于哪个父任务和哪一天完成。
_Avoid_: 独立任务, 子任务任务链

**未完成池**:
按 **任务链** 去重汇总存在未完成历史或已废弃历史的任务集合，用于回顾未完成历史、发起后续 **延续复制** 或取消 **废弃**。
_Avoid_: 逾期列表, 失败列表

**未完成明细**:
**未完成池** 中某个任务链下所有 **未完成**、**已延续** 或 **已废弃** 日轨迹的日期化记录。
_Avoid_: 重复任务, 多条任务

**活跃日轨迹**:
某个 **任务链** 当前唯一仍可被完成、延续、变更、回池或废弃的非历史 **日轨迹**。
_Avoid_: 当前任务, 活跃任务

**已延续待完成**:
**未完成池** 中某个任务链已存在后续 **活跃日轨迹**，但该活跃轨迹尚未完成的展示状态。
_Avoid_: 已处理, 已移出未完成池

**排期**:
将 **任务池** 中的任务安排到某个具体 **Day Todo** 并生成第一条 **日轨迹** 的操作。
_Avoid_: 加入清单, 放到日期

**任务定义**:
用户对一件任务当前承诺的标题、描述和边界。
_Avoid_: 任务内容, 可编辑字段

**任务来源**:
任务形成时的文档级来源，区分用户直接创建、烛龙建议后用户确认和数据导入等；核心任务保留最小来源，烛龙详细过程留在对应会话与 **AI 应用批次**。
_Avoid_: 当前编辑者, AI 自动创建, 生产力权重

**任务描述**:
任务的背景、目标或范围说明。进入 **Day Todo** 后会成为 **日轨迹** 的描述快照，历史日不可覆盖式改写。
_Avoid_: 可随意覆盖的备注, 任务标题补丁

**任务附言**:
任务上的临时想法、提醒或补充说明。进入 **Day Todo** 后会成为 **日轨迹** 的附言快照，历史日不可覆盖式改写。
_Avoid_: 任务状态, 复盘文本

**子任务**:
附属于某条 **日轨迹** 的任务细化项，用于补充而不是覆盖 **任务定义**。
_Avoid_: 子链路, 子待办

**子任务难度权重**:
子任务参与任务进度计算的权重，第一期取值为简、中、难。
_Avoid_: 优先级, 工时估算, 状态

**子任务轨迹线**:
同一个 **子任务** 在多次 **延续复制** 后跨日期保持的连续身份，用于展示它在哪一天开始、延续到哪些天、在哪一天完成或废弃。
_Avoid_: 子任务链, 独立任务链

**部分完成**:
某条 **日轨迹** 中已有部分 **子任务** 完成、但仍存在未完成子任务时的派生展示标签，不是父任务的正式状态。
_Avoid_: 半完成状态, 父任务状态

**手动完成进度**:
没有 **子任务** 的 **日轨迹** 上由用户手动维护的 0 到 100 进度值。
_Avoid_: 完成状态, 可回退历史进度

**加权子任务进度**:
存在 **子任务** 时由 **子任务难度权重** 和完成情况自动计算的任务进度。
_Avoid_: 手填百分比, 子任务数量比例

**进度下限**:
同一 **任务链** 延续到后续日期后，后续 **日轨迹** 的展示进度不能低于历史已达进度。
_Avoid_: 可回退进度, 重置进度

**日历轨迹总览**:
按自然月展示每日任务数量、完成热度和任务摘要的视图入口。
_Avoid_: 月报, 统计大屏

**任务链**:
同一件事在排期、延续和变更过程中保持的连续身份。
_Avoid_: 原任务, 主任务

**日轨迹**:
任务一旦进入某个 **Day Todo** 后在该日期留下的不可删除历史记录。
_Avoid_: 日志, 快照, 历史副本

**日结束**:
按用户设备本地时区在自然日 00:00 切换后，前一日 **Day Todo** 进入锁定状态的边界。
_Avoid_: 结算, 截止, 关账

**锁定 Day Todo**:
已经过了 **日结束** 边界、不可再删除或无痕改期的 **Day Todo**。
_Avoid_: 归档清单, 历史列表

**未来 Day Todo**:
日期晚于用户设备当前自然日、可提前排期但尚不能完成或结算的 **Day Todo**。
_Avoid_: 计划清单, 未来列表

**计划草稿**:
尚未到达其日期的未来任务安排，可在未来日期之间调整且不形成延续轨迹。
_Avoid_: 日轨迹, 历史任务

**未来计划**:
跨日期汇总所有 **计划草稿** 的聚合视图。
_Avoid_: 未来任务池, 待排期任务

**每日复盘**:
附属于某个 **Day Todo** 的当日任务结果统计和用户手写总结。
_Avoid_: 周报, 数据大屏, 统计页

**复盘来源**:
**每日复盘** 文本的文档级创作来源，区分用户独立撰写、烛龙起草后原样确认和烛龙起草后用户修改；核心复盘保留最小来源标记，完整过程留在对应会话。
_Avoid_: 逐字归因, AI 可信度评分, 删除 sidecar 后抹除来源

**本地优先**:
用户的任务轨迹以本机数据为主，即使没有账号或网络也能完整使用核心功能。
_Avoid_: 离线模式, 单机版

**在线优先**:
用户选择由在线服务作为主要数据运行路径；本机仍可保留缓存和可恢复导出，但不作为主同步事实源。
_Avoid_: 网页版, 必须联网才能打开

**数据模式**:
晷迹在同一时间只启用一种主数据运行路径：**本地优先** 或 **在线优先**。两种模式可以切换，但不能并行写入同一份任务事实。
_Avoid_: 双模式同时运行, 混合同步模式

**同步端点**:
用户选择或系统内置的跨设备数据交换位置，例如 iCloud、Dropbox、本地同步文件夹或对象存储。
_Avoid_: 同步目录, 云盘目录

**定时备份**:
按计划把当前任务事实导出成可恢复 **数据包**，写入用户配置的 iCloud Drive、S3 或其他对象存储。它是旁路备份，不是双向同步，也不是第二个主数据模式。
_Avoid_: 自动双向同步, 第二事实源, 数据库文件同步

**同步端点占位**:
第一期设置页中展示但暂不启用的同步能力入口，例如自定义同步端点、iCloud 云同步、S3、WebDAV 和本地同步文件夹。
_Avoid_: 已可用同步, 假同步

**数据包**:
晷迹用于手动导出、导入或未来同步的数据交换文件，承载可校验的任务轨迹数据。
_Avoid_: 数据库文件, 备份文件

**应用偏好**:
本机 UI 偏好设置，例如外观主题和界面语言。
_Avoid_: 同步事实, 任务数据

**外观主题**:
Mac UI 的视觉主题，第一期包含冷灰和微暖纸感。
_Avoid_: 任务分类, 数据状态

**界面语言**:
Mac UI 的显示语言，第一期包含中文和 English。
_Avoid_: 任务内容语言, 翻译任务

**原生云同步**:
晷迹未来提供的内置账号和云端同步能力。
_Avoid_: 第三方同步, 备份目录

**烛龙 AI**:
晷迹中的可选 AI Agent，通过 **任务成形闭环** 和 **每日收尾闭环** 协助用户把模糊意图变成可信计划，并依据真实轨迹修正下一次承诺；它不影响普通清单功能的完整可用性。
_Avoid_: 核心任务引擎, 自动驾驶, 聊天页

**烛龙本地模式**:
没有可用 **AI Provider** 时仍保留承诺统计、风险本地基准、记忆、事件历史、会话准备和既有草稿审查的工作状态，但不生成 LLM 追问、情境判断、规划草稿或记忆候选。
_Avoid_: 隐藏烛龙入口, 固定模板冒充 AI, 本地证据冒充模型结果

**烛龙 sidecar 数据**:
烛龙记忆、画像、会话、规划草稿、事件历史、校准数据和 Provider 发送记录等高敏感旁路资料，默认只保存在产生它们的设备，不进入普通 Todo 数据包或同步；未来导出与跨设备同步必须单独授权并加密。
_Avoid_: 核心任务事实, 默认云同步, 普通数据包附带

**烛龙 sidecar 加密**:
使用 Keychain 中的应用独立密钥对 **烛龙 sidecar 数据** 执行的应用层静态加密；密钥或加密能力不可用时烛龙必须 fail-closed，不能回退明文，普通 Todo 继续可用。
_Avoid_: FileVault 替代, 明文降级, API Key 复用

**每日收尾闭环**:
用户结束一天时，由烛龙基于已授权的任务轨迹协助复盘事实、处置未完成任务，并形成下一次 **可信承诺** 的场景；它可以在日结束前后运行，但不控制日结束，也不改写锁定日轨迹。
_Avoid_: AI 日报, 自动改期, 无确认排程

**每日收尾提醒**:
用户可配置的一次性本地提示，只邀请用户开始或补做 **每日收尾闭环**；它不读取会话数据、不调用 Provider、不自动生成复盘或排期，并可完全关闭。
_Avoid_: 自动开始会话, 后台 AI 调用, 重复催促

**未完成原因假设**:
烛龙依据任务轨迹提出、等待用户确认或修正的可能原因；它不是任务事实，未经确认不能写入复盘或成为 **已确认记忆**。
_Avoid_: 原因结论, 心理诊断, 人格标签

**未决后续**:
**每日收尾闭环** 中用户尚未决定后续安排的任务记录，只存在于 **烛龙会话**，不形成新的任务状态，也不改变日结束结算。
_Avoid_: 待定状态, 自动延续, 强制处置

**任务成形闭环**:
用户提出新的模糊任务时，由烛龙通过交互式追问明确目标、关键取舍与约束，再形成可检查 **AI 规划草稿** 的场景。
_Avoid_: 一键拆解, 通用计划模板, 替用户决定目标

**规划自治**:
用户明确目标、关键取舍与约束后，授权烛龙自主完成任务拆解、排序、风险检查和初步排期，但产物仍是等待确认的 **AI 规划草稿**。
_Avoid_: 目标自治, 无约束规划, 自动写入 Todo

**规划简报**:
烛龙在 grill 结束前汇总的目标、完成标准、硬约束、用户取舍、授权自主事项、显式假设、未决问题和数据范围，必须由用户审查后才能发起 **规划委托**。
_Avoid_: 隐藏 system prompt, 自动进入规划, 对话摘要替代品

**规划委托**:
用户针对当前 **烛龙会话** 和 **规划简报** 版本授权烛龙进入 **规划自治** 的单次动作，不包含 Todo 写入或长期托管权限；简报实质变化后必须重新委托。
_Avoid_: 批量确认, AI 托管授权, 自动跨阶段

**滚动式规划**:
烛龙展示目标的完整阶段地图，但只把近期、信息充分且可承诺的范围细化为 Todo，并在取得新证据或完成阶段后生成新的 **AI 规划草稿** 版本。
_Avoid_: 一次性穷尽计划, 远期伪精确, 假设性 Todo 泛滥

**可行性审查**:
烛龙在提出承诺前，依据明确的完成标准、**可验证世界约束**、资源硬约束和可比历史检查计划；结果只能是硬约束不可行、带概率区间的高风险承诺或证据不足。
_Avoid_: 模型感觉, 固定任务数阈值, 伪工时容量

**可验证世界约束**:
带来源、适用对象、版本、单位、不确定性和计算规则的事实或保守边界，可以来自 **世界约束库** 或对当前对象的本地测量，并能参与硬约束推导。
_Avoid_: 模型记忆, 无版本常识, 未引用经验值

**世界约束库**:
保存和版本化 **可验证世界约束** 的白盒知识集合，由随 App 审查发布的内置条目、对当前对象的本地测量和用户明确声明的适用约束组成；LLM 可以检索和解释，但只能提出 **世界约束候选**。
_Avoid_: system prompt 常识列表, 模型内置世界观, 无来源规则库

**世界约束候选**:
LLM 提出的待审查硬知识或测量建议，在补齐来源、适用范围、版本和计算规则并由用户或项目审查确认前，不能参与硬约束推导。
_Avoid_: 已激活约束, 模型常识升级, 自动规则更新

**硬约束不可行**:
目标的保守工作量下界已经超过可用时间、预算或其他物理资源，或者必要依赖尚不存在的审查结果。
_Avoid_: 个人偏好, 低完成率, 模型不推荐

**高风险承诺**:
计划未违反硬约束，但在样本充分的个人化可比历史中，兑现概率区间低于用户 **承诺风险阈值** 的审查结果。
_Avoid_: 不可行, 人格判断, 单日任务数告警

**承诺风险阈值**:
用户明确选择的最低可接受兑现概率，可以保存为默认偏好并在单次 **烛龙会话** 中覆盖，用于判断是否形成 **高风险承诺**。
_Avoid_: 系统科学常数, 用户能力评分, 隐藏默认值

**可信承诺**:
目标、完成标准和关键约束已经明确，不违反已知硬约束，证据、假设、风险和不确定性对用户可见，并由用户知情确认且可用真实结果校准的承诺。
_Avoid_: AI 认可计划, 高完成率目标, 少任务计划

**承诺风险计算器**:
在本机以可审计、可版本化的固定规则计算个人历史基准率、概率区间和证据资格的组件；相同输入和版本必须得到相同结果，并允许输出 **证据不足**。
_Avoid_: LLM 概率, 黑盒评分, 生产力分数

**LLM 情境判断**:
烛龙基于任务语义、依赖和当前约束提出的情境风险说明，必须披露依据、假设和反例；在通过真实后验校准前，它不能生成或修改正式兑现概率。
_Avoid_: 历史基准率, 未校准概率, 模型直觉分数

**双通道承诺风险**:
同时展示 **承诺风险计算器** 的本地历史基准与 **LLM 情境判断** 的结果，两者在 LLM 概率通过后验校准前不合并成单一数字。
_Avoid_: LLM 最终裁决, 隐藏加权分数, 伪综合概率

**LLM 概率校准资格**:
某一具体 **Provider 配置身份** 和预测配置经过真实结果验证后，获准参与正式综合概率的状态；模型、prompt、schema、本地算法、工作量度量或 reference class 规则实质变化时必须重新积累。
_Avoid_: 跨模型继承, 永久资格, Provider 通用校准

**实验概率**:
尚未获得 **LLM 概率校准资格** 的预测配置为后验验证生成的概率，只能在校准与审计面板中查看和积累，不能进入主决策界面或影响正式建议。
_Avoid_: 正式综合概率, 隐藏预测, 用户锚定数字

**综合概率授权**:
用户允许某个已获得 **LLM 概率校准资格** 的预测配置参与正式综合概率的可撤销授权；资格失效或配置实质变化时必须自动暂停。
_Avoid_: 资格自动启用, 强制实验概率, 失准后继续生效

**承诺快照**:
用户手动确认或命中 **自动确认授权** 时保存的任务集合、任务版本、**预计工作量区间**、确认时间和确认来源，用于事后评估原计划，日内新增任务不能反向改写它。
_Avoid_: 最终 Day Todo 内容, 自动午夜快照, 可覆盖计划

**确认今日计划**:
用户在 **Day Todo** 中选择真正准备于该日完成的任务并生成 **承诺快照** 的本地核心可选动作，不依赖 **烛龙 AI**、**AI Provider** 或网络，也不阻塞 **快速捕获**。
_Avoid_: 锁定 Day Todo, 强制承诺, AI 创建关卡

**自动确认授权**:
用户预先设置触发时间和纳入范围、允许系统自动生成 **承诺快照** 的可撤销规则；自动与手动快照必须标记来源并分别校准，撤销只影响未来快照。
_Avoid_: 默认自动承诺, 隐式计划确认, 混合来源样本

**承诺兑现**:
将 **承诺快照** 与日结束结果比较所得的计划结果；完成计为兑现，变更单列为承诺调整，部分推进不等于完整兑现，其他结算表示原日期承诺未兑现。
_Avoid_: 实际产出, 生产力评分, 人格评价

**实际产出**:
某日真实完成或推进的全部工作，包括承诺任务、日内新增任务和变更后的替代任务，必须与 **承诺兑现** 分别展示且不能互相抵销。
_Avoid_: 承诺兑现, 忙碌指数, 计划成功率

**证据不足**:
任务规模、可用资源或可比历史不足以支持可行性结论的审查结果，烛龙必须披露缺失证据和取得结论所需条件，并继续追问或明确保留不确定性。
_Avoid_: 默认可行, 默认不可行, 补造数据

**预计工作量区间**:
用户在主动请求容量规划时，对任务所需投入给出的范围估计；它独立于 **子任务难度权重**，可以由烛龙提出但必须由用户确认。
_Avoid_: 精确工时承诺, 子任务难度, 创建至完成时长

**可投入时间区间**:
用户在主动请求容量规划时，对某日可用于当前计划的时间范围声明，可以标注会议、出行或健康等异常条件。
_Avoid_: 全天时长, 自动推断空闲, 强制计时

**闭域规划**:
烛龙只使用用户主动提供的资料和已授权的晷迹任务轨迹形成 **AI 规划草稿**；外部未知事实必须转为追问、显式假设或调查任务。
_Avoid_: 隐式浏览, 任意工具调用, 外部执行

**烛龙会话**:
烛龙围绕一个主要意图持续进行澄清、规划、审查和应用的有界工作单元，可以暂停、恢复、版本化和归档；消息不可逐条改写或删除，用户可以追加 **会话更正**，也可以整体删除会话。
_Avoid_: 无限聊天线程, 多目标混杂, 隐式跨会话上下文

**会话工作空间**:
围绕当前 **烛龙会话** 展示单题澄清、用户决定、证据、假设、规划版本和 Todo 变更预览的主界面，辅以可收起的上下文检视和事件历史入口。
_Avoid_: 通用聊天页, 能力按钮 Dashboard, 草稿收件箱

**会话更正**:
用户或烛龙对既有会话内容追加的修正记录，**会话摘要** 使用最新更正，但原内容和更正关系继续保留。
_Avoid_: 编辑旧消息, 删除错误决定, 静默改写摘要

**会话删除**:
用户整体删除一场 **烛龙会话** 及其未应用内容的操作，不撤销已经形成的 Todo 历史，也不自动删除另行确认的 **已确认记忆**；**烛龙事件历史** 只保留无正文删除标记。
_Avoid_: 逐条消息删除, Todo 撤销, 隐式删除记忆

**会话摘要**:
从本机完整 **烛龙会话** 中提炼的用户可审查、可版本化上下文，记录目标、用户决定、约束、烛龙假设和未决事项；远程调用默认只发送已披露的摘要版本、相关原文和 **会话记忆范围**。
_Avoid_: 隐藏压缩, 全量对话上传, 覆盖旧摘要

**权限上限**:
用户在设置中按禁止、询问或允许配置的数据读取、记忆使用、远程发送和建议应用边界，任何 **会话数据授权** 都不能超过它。
_Avoid_: 二元总开关, 隐藏默认权限, 会话自行提权

**会话数据授权**:
单场 **烛龙会话** 在 **权限上限** 内获得的数据与 Provider 使用范围；范围不变时可以连续使用，扩大范围、切换 Provider 或过期时必须重新授权。
_Avoid_: 跨会话无限授权, 隐式扩权, Provider 通用许可

**YOLO 权限**:
用户针对当前 **Provider 配置身份** 一次放开本地读取、记忆使用和远程发送权限并允许新会话自动继承的高便利模式；身份实质变化时自动暂停，它仍须披露和记录所有行为，第一阶段不能放开无人确认的 Todo 写入。
_Avoid_: 全权管家模式, 隐身模式, 自动写库

**烛龙事件历史**:
按时间记录重要 AI 数据使用、远程发送、假设、决定、记忆变更、授权、Todo 操作和失败的用户可读时间线；系统和用户都不能修改、重排或逐条删除事件，用户只能按连续时间范围执行 **事件清理**。该约束是 App 领域层的 append-only 语义，不宣称抵抗设备所有者直接改写本机数据库。
_Avoid_: 纯 Toast, 调试日志, 敏感正文副本, tamper-proof 宣称

**事件清理**:
用户主动删除最近一段连续时间范围内全部烛龙事件内容的操作，不允许选择性删除单条事件，也不撤销事件已经造成的 Todo 历史事实；清理完成后必须追加一条记录起止范围、事件数量和操作时间的无内容清理标记。
_Avoid_: 逐条删除, 日志改写, 操作撤销

**烛龙记忆**:
用户明确开启后，烛龙在本机从历史任务和交互中提炼的可管理记忆项集合；每项记忆区分用户陈述与烛龙推断，并可追溯、修正、停用或遗忘。
_Avoid_: 黑盒画像, 原始对话仓库, 默认远程上传

**精粹用户画像**:
从当前 **已确认记忆** 重新生成的可追溯汇总视图，不是独立事实或永久身份；每项结论必须能展开到来源记忆，记忆变化或冲突时必须更新或标明不确定。
_Avoid_: 原子记忆替代品, 隐藏人格分数, 不可修改身份

**已确认记忆**:
用户明确要求记住或已经审查确认的 **烛龙记忆** 项，可以在后续 **烛龙会话** 中使用，并始终允许用户查看、修改或删除。
_Avoid_: 隐式画像, 不可编辑偏好, 永久标签

**记忆候选**:
烛龙根据任务轨迹或交互提出、但尚未获得用户确认的潜在记忆项；它必须展示证据窗口、置信度和反例，且不能在后续会话中被当成用户事实。
_Avoid_: 已确认记忆, 自动生效画像, 隐藏推断

**记忆刷新**:
在每日收尾、任务成形会话结束、用户重要更正、主动请求或明确周期规则等检查点，批量比较新证据与既有记忆并产生 **记忆候选** 的过程；单次任务操作和 YOLO 权限都不能让推断自动成为 **已确认记忆**。
_Avoid_: 实时画像漂移, 每次操作推断, 自动确认候选

**记忆冲突**:
新证据或用户更正与现有 **已确认记忆** 不一致时保留的待裁决关系，不能覆盖旧内容；未解决期间相关记忆默认不参与规划，确认变化后创建带生效时间的新版本。
_Avoid_: 自动覆盖, LLM 静默选边, 冲突记忆同时生效

**记忆抑制规则**:
用户选择删除记忆并停止同类推断后保留的最小化本机规则，用于阻止烛龙再次提出该类 **记忆候选**，但不保存已删除的记忆正文。
_Avoid_: 已删除记忆副本, 隐藏记忆, 不可撤销封锁

**会话记忆范围**:
从 **烛龙记忆** 中为当前 **烛龙会话** 选出的相关记忆项集合，使用原因和远程发送内容必须对用户可见并允许排除。
_Avoid_: 全量记忆注入, 隐式跨会话读取, 不可见系统画像

**AI Provider**:
用户配置的模型服务来源，例如 OpenAI-compatible endpoint、本地模型或自定义 HTTP provider。
_Avoid_: 固定模型, 内置账号

**Provider 配置身份**:
由 Provider ID、类型、Base URL、本地或远程性质、模型及数据发送能力共同确定的信任身份，供权限和概率校准绑定；任一实质变化都形成需要重新确认的新身份。
_Avoid_: 显示名称, Provider 品牌通用信任, 同名配置继承

**Provider 发送记录**:
单次 Provider 调用在对应 **烛龙会话** 中保存的凭证脱敏请求与响应记录，包含实际发送内容、上下文版本、Provider 配置身份和结果；**烛龙事件历史** 只保存摘要与关联入口。
_Avoid_: API Key 日志, 事件正文副本, 仅范围标签

**AI Provider Registry**:
烛龙 AI 用于注册、选择和健康检查 **AI Provider** 的旁路组件。
_Avoid_: 模型硬编码, 全局单例凭证

**AI 数据范围快照**:
用户明确授权给烛龙 AI 分析的一次性数据范围，可来自 **Day Todo**、**任务池**、**未完成池**、**已完成池**、**标签**、**会话摘要** 或 **会话记忆范围**。
_Avoid_: 全库扫描, 自动上传, 数据库备份

**本地证据报告**:
烛龙 AI 在调用 provider 前，先由本机结构化任务轨迹计算出的事实集合，例如延续次数、完成率、未完成任务链数量和部分完成。
_Avoid_: AI 猜测, 模型判断, 聊天总结

**本地广算、远程少发**:
在用户授权下由本机从广泛历史生成统计与证据，但只向 Provider 发送当前意图必需的日期、相关任务链、选定记忆、会话内容和聚合结果的数据最小化原则。
_Avoid_: 原始历史全量上传, 用远程模型做本地统计, 范围自动扩张

**AI 建议草稿**:
烛龙 AI 生成但尚未被用户确认的任务、子任务、排期、主分类、标签或复盘建议。
_Avoid_: 自动修改, 已执行操作

**AI 分类候选**:
烛龙在 **AI 建议草稿** 中提出的现有分类复用或新分类建议，必须包含类型、理由和近似重复项；用户确认前不进入分类库，也不产生任务关联。
_Avoid_: 已创建分类, 后台分类, 自动扩张分类库

**批量分类草稿**:
烛龙针对用户明确圈定的多条任务生成的逐项分类变更集合，允许用户修改或排除任意项；最终选中内容作为一个 **AI 应用批次** 原子应用，每项保留自己的 **分类变更记录**。
_Avoid_: 摘要式批改, 自动全库分类, 部分成功

**用户确认分类关系**:
用户直接设置或确认 **AI 分类候选** 后形成的普通当前分类关系；来源继续可追溯，但不赋予烛龙所有权或未来修改权限。
_Avoid_: AI 所有标签, 临时 AI 分类, 自动可改关系

**AI 复盘草稿**:
烛龙在 **每日收尾闭环** 中生成、等待用户审查和编辑的复盘文本，与 **Todo 变更 diff** 分开确认，不能自动保存或夹带未确认的原因归因。
_Avoid_: 已保存复盘, Todo 批量确认, AI 自动总结

**AI 规划草稿**:
烛龙在 **任务成形闭环** 中生成的结构化规划记录，由阶段地图、**AI 决策说明** 和可编辑 **Todo 变更 diff** 组成；任务结构与日期建议可以分别确认，它可以持久化并关联确认后生成的任务，但不拥有这些任务的生命周期。
_Avoid_: 计划草稿, Project, 已写入任务

**Todo 变更 diff**:
**AI 规划草稿** 中逐项列出将创建或调整哪些任务、子任务、排期和其他合法 Todo 操作的可编辑写入清单，是 **批量确认** 的唯一执行依据。
_Avoid_: 规划文章, 隐藏副作用, 从聊天文本直接执行

**过期规划草稿**:
生成时依赖的任务版本、日期计划、记忆范围或风险证据已经实质变化的 **AI 规划草稿**，可以继续查看和审计，但不能应用；按当前状态刷新必须生成新版本。
_Avoid_: 自动覆盖, 静默 rebase, 继续应用旧建议

**用户修订版本**:
用户编辑 **Todo 变更 diff** 后形成的独立 **AI 规划草稿** 版本，必须保留 AI 原版并标明修改来源；本地风险重新计算，受影响的旧 **AI 决策说明** 不再适用。
_Avoid_: 覆盖 AI 原版, 伪装 AI 建议, 沿用失效理由

**AI 决策说明**:
烛龙为自主判断提供的用户可审查说明，区分用户决定与烛龙假设，并明确列出数据范围、所用证据、适用约束、主要备选方案、取舍理由、不确定性、预期影响和所需授权。
_Avoid_: 原始模型思维链, 黑盒评分, 无依据结论

**AI 托管授权**:
用户预先授予烛龙 AI 在指定范围内自动应用某些建议草稿的未来权限策略；第一阶段不启用，只允许用户对当前具体草稿执行 **批量确认**。
_Avoid_: 无限制自动驾驶, 默认全权, 模型裸写库

**全权管家模式**:
**AI 托管授权** 的未来高权限形态，允许烛龙 AI 在用户设定的范围、操作类型、次数和有效期内自动处理任务；第一阶段不提供。
_Avoid_: 绕过状态机, 永久授权, 不留痕执行

**应用 AI 建议草稿**:
用户确认 **AI 建议草稿** 后，将其转换为普通领域接口调用的过程。
_Avoid_: AI 自动执行, 后台写入, 绕过确认

**批量确认**:
用户审查当前具体 **AI 规划草稿** 的依据、假设和全部 Todo 变更后，以一次操作授权应用其中一组合法变更；它不是 **AI 托管授权**，也不允许后台无人执行。
_Avoid_: 逐项点击要求, 全权管家, 未预览批处理

**原子应用**:
**批量确认** 的全部 Todo 变更必须在一个本地事务中全部成功或全部回滚；用户若只想应用一部分，必须在确认前主动拆分批次。
_Avoid_: 部分成功, 失败后继续, 静默跳过

**AI 应用批次**:
一次 **原子应用** 成功后保存的来源草稿版本、最终 diff、结果实体和应用时间记录，在全部结果仍符合 **有限撤销** 时可以作为整体撤销。
_Avoid_: 独立 Todo 状态, 可改写历史, 部分应用结果

**批次撤销**:
在 **AI 应用批次** 的全部结果尚未产生后续依赖或历史事实时执行的原子撤销；失效后只能使用合法普通领域操作，原应用与撤销事件都继续保留。
_Avoid_: 历史回滚, 部分撤销, 删除应用事件

**分类身份**:
一个 **主分类** 或 **标签** 在创建后保持稳定的领域身份，名称变化不能替换它；历史分类快照以该身份关联当时名称与当前名称。
_Avoid_: 分类名称, 显示文本, 分类合并关系

**主分类**:
一条 **任务链** 当前可拥有的单值稳定组织归属，与多个对等 **标签** 相互独立；它不属于任务状态。
_Avoid_: Tag I, 主 Tag, 标签, Tag, Label, 状态, 清单, 文件夹

**主分类快照**:
某条 **日轨迹** 成为历史事实时保留的当时 **主分类**；后续调整当前主分类不能改写它。
_Avoid_: 当前主分类, 动态分类, 历史重算

**标签**:
用户或烛龙 AI 附加在 **任务链** 上的零到多个对等分类词，用于组织和分析；标签之间无主次顺序和产品语义数量上限，也不能充当 **主分类**。
_Avoid_: Tag, Label, Tag I, Tag II, Tag III, 主 Tag, 主分类, 状态, 清单, 文件夹

**标签快照**:
某条 **日轨迹** 成为历史事实时保留的当时全部 **标签**；后续调整当前标签不能改写它。
_Avoid_: 当前标签, 动态标签, 历史重算

**历史分类更正**:
对既有 **主分类快照** 或 **标签快照** 追加的显式修正关系，原快照继续保留且可见。
_Avoid_: 覆盖历史, 静默改写, 当前分类调整

**分类变更记录**:
每次创建、关联、移除、继承、改名、归档、合并或 **历史分类更正** 留下的来源、时间和前后值记录；烛龙来源还必须关联会话、草稿版本、建议依据和用户确认。
_Avoid_: 当前分类关系, 调试日志, 无来源修改

**分类改名**:
在不改变含义和 **分类身份** 的前提下调整 **主分类** 或 **标签** 的当前名称；历史分类快照保留当时名称并可关联现名。
_Avoid_: 分类合并, 语义替换, 覆盖历史名称

**分类继承**:
**延续复制** 与 **回池** 保留同一 **任务链** 的当前主分类和标签；**变更** 或复制为新任务时，新任务链默认复制这些当前关系并披露来源，但不复制任何历史分类快照。
_Avoid_: 历史快照复制, 隐式 AI 分类, 新旧任务链共用关联

**分类关系历史**:
一个 **主分类** 或 **标签** 曾经与某条 **任务链** 建立、替换或解除关系的不可覆盖事实；解除当前关系不会抹掉其曾被使用的事实。
_Avoid_: 当前分类关系, 可删除操作日志, 分类变更记录

**分类硬删除**:
彻底移除一个从未产生 **分类关系历史**、从未进入历史分类快照且未参与 **分类合并** 的 **主分类** 或 **标签**；旧身份仍保留最小删除标记，不能被离线数据复活。
_Avoid_: 分类归档, 分类合并, 删除历史关联, 复活旧分类身份

**分类归档**:
停止某个 **主分类** 或 **标签** 接受新关联的生命周期状态；既有关联与历史分类快照继续保留。
_Avoid_: 分类硬删除, 隐藏历史, 自动解除关联

**分类合并**:
用户确认 **分类合并预览** 后，把来源项的当前关联原子迁移到同类目标项并将来源项标记为 **已合并分类**；统计可以按目标项聚合，原始历史分类快照仍可查看。
_Avoid_: 重命名, 覆盖历史, 跨类型合并

**分类合并预览**:
执行 **分类合并** 前供用户审查的影响视图，列明受影响的当前任务、历史分类快照和需要去重的关联。
_Avoid_: 合并结果, 隐式批量修改, 普通确认弹窗

**已合并分类**:
完成 **分类合并** 后的来源 **主分类** 或 **标签**，不再接受新关联，但继续保留到目标项的关系和历史查阅入口。
_Avoid_: 分类归档, 分类硬删除, 目标分类

**回池**:
将已经产生过 **日轨迹** 的任务重新放回 **任务池**，同时在原日期保留其历史记录的操作。
_Avoid_: 取消, 删除, 撤回

**变更**:
当用户要改变已有 **任务定义** 时，将旧任务标注为已变更并自动生成新任务的操作。
_Avoid_: 编辑, 覆盖, 改名

**变更指针**:
旧 **日轨迹** 在执行 **变更** 后指向同日新 **日轨迹** 的只读关联，用于展示“被变更为”。
_Avoid_: 重命名记录, 可编辑跳转, 覆盖引用

**已变更**:
旧任务在执行 **变更** 后进入的状态，表示它已被另一个新任务定义接替。
_Avoid_: 已完成, 已取消, 已删除

**待完成**:
任务进入某天 **Day Todo** 后尚未处理的状态。
_Avoid_: 待办, 未开始

**已完成**:
任务在某天 **Day Todo** 中被用户确认完成的状态。
_Avoid_: done, 达成

**未完成**:
任务在 **日结束** 时仍未完成且未被延续、变更或回池的状态。
_Avoid_: 失败, 逾期

**已延续**:
任务通过 **延续复制** 产生后续日期任务后，原日期任务进入的状态。
_Avoid_: 已延期, 已复制

**已回池**:
任务通过 **回池** 回到 **任务池** 后，原日期任务进入的状态。
_Avoid_: 已取消, 已撤回

**废弃**:
终止某个 **任务链** 未来继续出现，但保留其所有历史 **日轨迹** 的操作。
_Avoid_: 删除, 取消, 清理

**已废弃**:
任务链被 **废弃** 后，其相关未完成历史在视图中呈现的最终状态。
_Avoid_: 已删除, 已取消, 已忽略

**重新启用**:
取消某个 **任务链** 的 **已废弃** 标记，让原 **日轨迹** 回到其所在日期的待处理或未完成状态；它不创建今日任务，也不产生 **延续复制**。
_Avoid_: 重新排期, 延续到今天, 恢复删除

**延续复制**:
将某日未完成或需要继续推进的任务复制到另一个日期，同时保留原日期 **日轨迹** 的操作。
_Avoid_: 移动, 改期, 无痕延期

**复制为新任务**:
从已有任务或日轨迹创建一个不继承原 **任务链** 的新任务。
_Avoid_: 延续, 重复任务

**延续次数**:
同一任务通过 **延续复制** 跨日期持续出现的累计次数。
_Avoid_: 复制次数, 拖延天数

## Relationships

- 一个 **Day Todo** 属于且只属于一个自然日。
- 一个 **Day Todo** 包含零个或多个 **日轨迹**。
- 一个 **Day Todo** 最多有一个 **每日复盘**。
- 一个 **日轨迹** 在所属 **Day Todo** 中有且只有一个 **当日优先级**。
- **查看排序** 不改变 **当日优先级**。
- **有限撤销** 不能删除或改写历史 **日轨迹**。
- **Mac UI 设计契约** 是第一期 Mac 端验收入口，不能被后端旧模型或实现方便性降级。
- **任务池** 包含零个或多个尚未产生 **日轨迹** 的任务。
- **已完成池** 按完成 **日轨迹** 逐条展示，不按 **任务链** 去重。
- **已完成池** 中每条完成记录必须附带 **已完成轨迹摘要**。
- **已完成池** 也必须能按完成日期展示 **已完成子任务记录**，并标注其父任务。
- **已完成轨迹摘要** 从同一 **任务链** 的不可删除 **日轨迹** 派生，不保存为可编辑事实。
- **未完成池** 按 **任务链** 去重展示存在未完成历史或已废弃历史的任务。
- **未完成明细** 展示同一 **任务链** 在不同日期留下的 **未完成**、**已延续** 或 **已废弃** 日轨迹。
- 同一 **任务链** 同一时间最多只能有一个 **活跃日轨迹**。
- **排期** 会让任务离开 **任务池**，并进入目标日期的 **Day Todo**。
- 一个 **任务链** 可以包含一个或多个 **任务定义**。
- 一个任务进入某个 **Day Todo** 后，必须产生一个 **日轨迹**。
- **任务描述** 和 **任务附言** 在 **任务池** 中属于 **任务定义** 的补充信息；进入 **Day Todo** 后必须保存为对应 **日轨迹** 的快照。
- 当前日和未来日的 **任务描述**、**任务附言** 可以补充修改，历史日只能只读展示。
- **子任务** 不独立形成 **任务链** 或 **日轨迹**，而是随父级 **日轨迹** 被保存。
- **子任务难度权重** 只影响 **加权子任务进度**，不改变 **当日优先级** 或任务状态。
- **子任务轨迹线** 不等于 **任务链**；它只能在父级 **任务链** 内表达子任务跨天延续。
- **部分完成** 只能由 **子任务** 统计派生，不进入第一期父任务状态集合。
- 当前 **日轨迹** 仍存在待完成或未完成 **子任务** 时，父任务不能标记为 **已完成**。
- 用户若不再需要某个待完成 **子任务**，必须把该子任务标记为 **已废弃**，不能无痕删除后完成父任务。
- 没有 **子任务** 的 **日轨迹** 使用 **手动完成进度**。
- 存在 **子任务** 的 **日轨迹** 使用 **加权子任务进度**，不能再手填进度。
- **延续复制** 产生的新 **日轨迹** 的进度不能低于 **进度下限**。
- **日历轨迹总览** 从 **日轨迹** 派生，不保存为独立事实。
- 已产生 **日轨迹** 的任务可以 **回池**，但原日期的 **日轨迹** 必须保留。
- 已产生 **日轨迹** 的 **任务定义** 不允许被覆盖式编辑。
- **变更** 会让旧任务和新任务同时出现在当天 **Day Todo** 中，旧任务标注“被变更为新任务”。
- **变更** 生成的新任务开启新的 **任务链**，旧 **日轨迹** 通过 **变更指针** 关联到新 **日轨迹**。
- **变更** 后旧任务当天状态为 **已变更**，新任务当天状态为待完成。
- 第一期 **Day Todo** 任务状态只包含 **待完成**、**已完成**、**未完成**、**已延续**、**已变更**、**已回池**、**已废弃**。
- **日结束** 后，前一日 **Day Todo** 变成 **锁定 Day Todo**。
- **日结束** 时仍处于 **待完成** 的任务会自动结算为 **未完成**。
- **日结束** 后，前一日的 **活跃日轨迹** 会变成历史 **日轨迹**，对应 **任务链** 进入 **未完成池**。
- **锁定 Day Todo** 中的 **日轨迹** 不可删除，也不可无痕移动到其他日期。
- **锁定 Day Todo** 中的 **当日优先级** 不可修改，但用户仍可使用 **查看排序** 按创建时间、首字母等方式浏览。
- **未来 Day Todo** 允许排期和调整 **当日优先级**，但不允许标记 **已完成** 或生成结算事实。
- **未来 Day Todo** 中尚未到达日期的任务属于 **计划草稿**，可在未来日期之间无痕调整，不产生 **延续复制**。
- **计划草稿** 在其日期到达后转为当前日期的 **日轨迹** 承诺。
- 任务被排期到 **未来 Day Todo** 后会离开 **任务池**，并出现在 **未来计划** 中。
- **计划草稿** 可以无痕回到 **任务池**，因为它尚未形成历史 **日轨迹**。
- **未来计划** 只汇总已排期到未来日期但尚未到期的 **计划草稿**。
- **计划草稿** 到达其日期后会自动从 **未来计划** 移出，并进入当天 **Day Todo**，状态为 **待完成**。
- 当前日期 **Day Todo** 中的任务可以在 **待完成** 与 **已完成** 之间切换。
- 当前日期 **Day Todo** 中的任务可以 **回池**，但必须留下状态为 **已回池** 的 **日轨迹**。
- 历史 **未完成** 任务不能 **回池**，只能 **延续复制** 或 **废弃**。
- **复制为新任务** 会创建新的 **任务链**，不增加原 **任务链** 的 **延续次数**。
- **锁定 Day Todo** 中的 **已完成** 状态不可撤销。
- **每日复盘** 包含自动统计和用户手写总结；它可以补写，但不能改写任务轨迹事实。
- 晷迹第一期遵循 **本地优先**，不要求账号、网络或 **原生云同步** 才能使用核心功能。
- 晷迹需要预留 **同步端点**，让用户未来可以选择第三方云盘、自托管存储或 **原生云同步**。
- 第一期只支持手动导出和导入 **数据包**，以及在设置中预留 **同步端点** 入口。
- 第一期不做自动双向同步，也不直接同步裸 SQLite 数据库文件。
- **应用偏好** 属于本机设置，不改变任何 **日轨迹** 历史事实。
- **本地优先** 下的 iCloud、S3、WebDAV 或本地同步文件夹必须表达为 **同步端点**，不是 **定时备份** 目标。
- **同步端点占位** 必须在设置页显示为规划中，不能伪装成已可用同步。
- **烛龙 AI** 是旁路 Agent；没有 **AI Provider** 时，Day Todo、任务池、未来计划、未完成池、已完成池和每日复盘仍必须完整可用。
- **AI Provider Registry** 只能管理 provider 配置、选择和健康状态，不能持有或改写任务事实。
- **AI 数据范围快照** 必须来自用户授权范围，不能默认扫描全库。
- **本地证据报告** 必须先于远程 provider 分析生成，烛龙的结论应引用这些证据。
- **烛龙 AI** 只能生成 **AI 建议草稿**，不能绕过普通领域接口直接改写任务事实。
- **AI 建议草稿** 被用户确认后，才可以通过 **应用 AI 建议草稿** 转化为普通任务、子任务、排期、延续、主分类、标签或复盘文本。
- **AI 托管授权** 可以让烛龙 AI 自动触发 **应用 AI 建议草稿**，但必须受范围、操作类型、次数和有效期限制。
- **全权管家模式** 仍然必须调用普通领域接口，不能绕过 **日轨迹** 状态机，也不能删除历史事实。
- **应用 AI 建议草稿** 必须调用普通领域接口，不能绕过 **日轨迹** 状态机。
- **标签** 不改变任务状态，也不能抹掉或重写已有 **日轨迹**。
- 一个 **日轨迹** 可以通过 **延续复制** 生成另一个日期的 **日轨迹**。
- 用户可以从任一未完成日期的 **日轨迹** 或 **未完成池** 对同一 **任务链** 发起 **延续复制**。
- **已延续** 是未完成历史被继续处理后的状态，仍应在 **未完成明细** 中展示。
- **延续次数** 随同一任务链路中的 **延续复制** 增加而增加。
- **废弃** 会让 **任务链** 在 **未完成池** 中标记为 **已废弃**；它仍然可见，但不能直接 **延续复制**。
- **废弃** 不删除任何历史 **日轨迹**。
- 用户可以对 **已废弃** 的任务链执行 **重新启用**；该操作只取消 **已废弃** 标记，不创建今日任务，也不复制 **子任务**。
- **任务链** 后续完成后，应从 **未完成池** 中移除，但其历史未完成 **日轨迹** 仍留在对应 **Day Todo**。
- **任务链** 的 **活跃日轨迹** 被标记为 **已完成** 后，整条 **任务链** 从 **未完成池** 移除。
- **任务链** 被 **延续复制** 后，在新的 **活跃日轨迹** 完成前仍显示在 **未完成池** 中，并呈现为 **已延续待完成**。
- **已延续待完成** 的任务链不能再次发起 **延续复制**，只能跳转到其 **活跃日轨迹** 或执行 **废弃**。
- **延续复制** 默认只复制未完成 **子任务** 到新 **日轨迹**，已完成 **子任务** 留在历史 **日轨迹** 中。
- **延续复制** 会将原日期未完成 **子任务** 标记为已延续，并在目标日期生成继承同一 **子任务轨迹线** 的新子任务。
- **已完成池** 的 **已完成轨迹摘要** 必须能展开展示每条 **子任务轨迹线**。
- 第一期仅支持 **有限撤销**；日结束结算、历史完成、历史延续、历史变更和废弃不能通过撤销抹掉，但已废弃任务链可通过 **重新启用** 取消废弃标记。

## Example Dialogue

> **Dev:** “用户今天把一个任务排进 **Day Todo**，晚上没做完，可以直接改成明天吗？”
> **Domain expert:** “不可以无痕改期。今天的 **日轨迹** 必须保留，只能用 **延续复制** 复制到明天，并显示这是第几次延续。”

> **Dev:** “一个还没决定哪天做的任务放在哪里？”
> **Domain expert:** “放在 **任务池**，它还没有进入任何 **Day Todo**，所以还没有形成 **日轨迹**。”

> **Dev:** “任务从 **任务池** 安排到今天后，还在任务池里吗？”
> **Domain expert:** “不在。这个动作叫 **排期**，任务会进入今天的 **Day Todo** 并生成 **日轨迹**。”

> **Dev:** “用户想把今天的任务标题改成另一个意思，可以直接编辑吗？”
> **Domain expert:** “不可以覆盖。要么添加子任务补充边界，要么做 **变更**：旧任务标注被变更为新任务，新任务自动生成，并且两个条目都留在当天 **Day Todo**。”

> **Dev:** “一个任务 Day 1 未完成，Day 2 延续后仍未完成，**未完成池** 里显示几条？”
> **Domain expert:** “显示一条，因为本质上是同一个 **任务链**；点开明细能看到它在 Day 1 和 Day 2 都留下了 **未完成** 日轨迹。”

## Flagged Ambiguities

- “移动”容易同时表示当天内排序和跨日期改期；已解决：当天内只表示排序、编辑和完成，跨日期必须使用 **延续复制**。
- “待办箱”来自 Todo清单竞品语言；已解决：晷迹使用 **任务池** 表示未排期任务集合。
- “编辑任务”容易表示覆盖已有 **任务定义**；已解决：已有 **日轨迹** 的任务不允许覆盖式编辑，只能添加子任务或执行 **变更**。
- “排序”容易同时表示历史优先级和临时浏览顺序；已解决：晷迹使用 **当日优先级** 表示可追溯承诺，使用 **查看排序** 表示 UI 临时排序。
