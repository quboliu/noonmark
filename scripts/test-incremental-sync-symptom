#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd -P)"
cd "$repo_root"

scripts/reset-dev-data audit
swift test \
  --filter IncrementalSyncPerformanceTests/testSteadyStateSQLiteDownloadOpensOnlyUnseenBatches
